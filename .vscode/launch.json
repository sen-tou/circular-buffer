{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Executable",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/ring_buffer",
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "stopOnEntry": false,
            "preLaunchTask": "Build Project"
        }
    ]
}
